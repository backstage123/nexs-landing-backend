{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    //"development": "Host=localhost; Database=nexs_landing; Port=5433; Username=postgres; Password=1234" //pgsql 16
    "development": "Host=localhost; Database=nexs_landing; Port=5434; Username=postgres; Password=1234", //pgsql 12
    "test": "Host=10.11.204.30; Database=nexs_landing; Port=5432; Username=postgres; Password=1234", //pgsql 12
    "testServerLocalhost": "Host=localhost; Database=nexs_landing; Port=5432; Username=postgres; Password=1234", //pgsql 12
    //production creds must not be here; If needed create a new production cred named "production" and set db connection to it but immediatly delete
    //after publishing the api and deploying it
  }
}
